# ifrit API 自动化测试 — 用户详细使用手册

> 本手册面向**测试人员、业务同学**编写，不要求编程基础。按章节顺序操作，即可从零完成：**配置环境 → AI 生成用例 → 带鉴权跑通测试 → 查看报告**。

---

## 目录

1. [这个工具能做什么](#1-这个工具能做什么)
2. [第一次使用（10 分钟上手）](#2-第一次使用10-分钟上手)
3. [文件放在哪里（只需知道这几个）](#3-文件放在哪里只需知道这几个)
4. [鉴权说明：手工用例 vs AI 用例](#4-鉴权说明手工用例-vs-ai-用例)
5. [方式 A：运行人工编写的用例](#5-方式-a运行人工编写的用例)
6. [方式 B：AI 一键生成并运行（推荐）](#6-方式-bai-一键生成并运行推荐)
7. [方式 C：AI 交互模式（像聊天一样操作）](#7-方式-cai-交互模式像聊天一样操作)
8. [冒烟测试（最快验证环境）](#8-冒烟测试最快验证环境)
9. [查看测试报告](#9-查看测试报告)
10. [清理日志与旧报告](#10-清理日志与旧报告)
11. [常见问题 FAQ](#11-常见问题-faq)
12. [命令速查表](#12-命令速查表)

---

## 1. 这个工具能做什么

| 能力 | 说明 |
|------|------|
| **跑接口测试** | 按 CSV/Excel/JSON 用例自动发请求、比对结果 |
| **AI 生成用例** | 从 Swagger / Apifox 文档自动生成大量测试用例 |
| **全局鉴权** | 自动登录拿 token，后续接口不用每条手写登录 |
| **测试报告** | 生成 HTML 报告，失败时可看请求详情 |
| **多环境** | 切换开发 / 测试 / 生产 API 地址 |

控制台只会显示简短摘要（`[IFRIT]` 开头），详细日志在 `logs/` 目录。

---

## 2. 第一次使用（10 分钟上手）

### 2.1 安装 Python

- 需要 **Python 3.10 或以上**
- Windows 安装后，打开「命令提示符」或 PowerShell，输入下面命令能显示版本即可：

```bash
python --version
```

若提示找不到命令，可改用：

```bash
py -3.10 --version
```

下文所有 `python` 命令，在 Windows 上若失败可替换为 `py -3.10`。

### 2.2 安装依赖

进入项目文件夹（含 `main.py` 的目录），执行：

```bash
pip install -r requirements.txt
```

### 2.3 配置个人密钥（复制 .env）

```bash
# Windows PowerShell
copy .env.example .env

# Linux / macOS
cp .env.example .env
```

用记事本打开 `.env`，至少填写：

| 配置项 | 是否必填 | 说明 |
|--------|----------|------|
| `OPENAI_API_KEY` | AI 生成时必填 | 大模型 API 密钥（OpenAI 或兼容网关） |
| `OPENAI_BASE_URL` | 可选 | 不填则用 `config/settings/ai.ini` 里的地址 |
| `OPENAI_MODEL` | 可选 | 不填则用配置文件里的模型名 |
| `IFRIT_BASE_URL` | 可选 | 个人临时覆盖被测 API 地址 |
| `DINGTALK_ACCESS_TOKEN` | 可选 | 仅开发同学 commit 通知用 |

> `.env` 不会上传到 Git，密钥只保存在本机。

### 2.4 确认被测 API 地址

团队默认地址在 `config/settings/env_config.ini`：

```ini
[environment]
base_url = http://192.168.31.129:5000
```

如需临时改用别的地址，在 `.env` 写：

```
IFRIT_BASE_URL=http://你的服务器:端口
```

### 2.5 验证环境是否通

```bash
python main.py --file fixtures/smoke/csv/api_test_smoke.csv
```

看到 `status=PASS` 即表示网络、Python、依赖均正常。

---

## 3. 文件放在哪里（只需知道这几个）

```
ifrit-apitest/
├── main.py                      ← 所有命令从这里进
├── .env                         ← 你的密钥（自己创建，勿提交）
├── config/settings/
│   ├── env_config.ini           ← API 环境地址
│   ├── auth.ini                 ← 全局登录账号（见第 4 节）
│   └── ai.ini                   ← AI 生成策略（一般不用改）
├── fixtures/
│   ├── manual/csv/              ← 人工编写的用例
│   ├── ai/csv/                  ← AI 生成的用例
│   └── smoke/csv/               ← 冒烟用例
├── api_docs/                    ← 本地 Swagger 文档
├── logs/                        ← 运行日志
└── reports/runs/                ← 每次测试的报告
```

**用例套件说明：**

| 套件 | 目录 | 适用场景 |
|------|------|----------|
| manual | `fixtures/manual/` | 人工维护、稳定回归 |
| ai | `fixtures/ai/` | AI 生成、探索性测试 |
| smoke | `fixtures/smoke/` | 快速验证环境 |
| all | 以上全部 | 一次性全量跑 |

---

## 4. 鉴权说明：手工用例 vs AI 用例

被测系统多数接口需要**先登录拿 token**。本框架提供 **两种方式**，可单独使用，也可组合。

### 4.1 当前默认登录账号

配置文件 `config/settings/auth.ini`：

| 项目 | 值 |
|------|-----|
| 登录接口 | `POST /api/login` |
| 用户名 | `test` |
| 密码 | `123456` |
| Token 注入方式 | 请求头 `Authorization: Bearer {{token}}` |

如需改账号，编辑 `auth.ini` 中 `[login]` 的 `body` 即可。

### 4.2 方式一：用例里自己写「登录步骤」（适合 manual 人工用例）

**特点：** 每条用例文件里**第一行往往是登录**，后面用 `{{token}}` 引用。

示例：`fixtures/manual/csv/test_case_addr_01.csv`

| 步骤 | 做什么 |
|------|--------|
| 第 1 条 | 调用 `/api/login`，从响应里提取 `token` |
| 第 2 条 | 调用 `/api/address/list`，headers/body 里写 `{{token}}` |

**运行命令（不需要 --global-auth）：**

```bash
python main.py --type csv --suite manual
```

或指定文件：

```bash
python main.py --file fixtures/manual/csv/test_case_addr_01.csv
```

> 这种方式**用例自包含**，适合演示「登录 + 业务接口」完整流程，也方便写负向用例（故意不传 token 等）。

### 4.3 方式二：全局鉴权 `--global-auth`（适合 AI 用例）

**特点：** 框架在跑测试前**自动登录一次**，之后所有需要鉴权的接口自动带上 token，**用例文件里不必再写登录行**。

配置来源：`config/settings/auth.ini`（同上 test/123456）。

**运行命令：**

```bash
python main.py --file fixtures/ai/csv/ai_address_business.csv --global-auth --suite ai
```

AI 生成的用例通常**不含登录行**，请**务必加 `--global-auth`**，否则会 401。

### 4.4 两种方式怎么选？

| 场景 | 推荐方式 | 命令要点 |
|------|----------|----------|
| 人工 CSV，内含登录步骤 | 用例内鉴权 | 不加 `--global-auth` |
| AI 生成的 CSV | 全局鉴权 | **必须** `--global-auth` |
| 人工用例也想简化 | 全局鉴权 | 去掉 CSV 里登录行 + `--global-auth` |
| 地址增删改查链路 | 全局鉴权 + 变量传递 | `--global-auth`，用例间保留 `address_id` |

### 4.5 鉴权失败怎么办？

1. 确认 `auth.ini` 里用户名密码正确（当前为 test/123456）
2. 确认 `env_config.ini` 里 `base_url` 能访问
3. 看 `logs/errors/` 里当天错误日志
4. 401 时框架会尝试自动重写鉴权配置（需本地有 `api_docs/apispec_1.json`）

---

## 5. 方式 A：运行人工编写的用例

### 5.1 跑全部 manual CSV

```bash
python main.py --type csv --suite manual
```

### 5.2 跑单个文件（含登录步骤的 manual 用例）

```bash
python main.py --file fixtures/manual/csv/test_case_addr_01.csv
```

### 5.3 跑单个文件 + 全局鉴权（省略 CSV 内登录时可选用）

```bash
python main.py --file fixtures/manual/csv/test_case_addr_01.csv --global-auth
```

### 5.4 成功时控制台示例

```
[IFRIT] ── 执行计划 ──
[IFRIT] 格式=csv 套件=manual 环境=environment
[IFRIT] 用例=2 文件=1 来源=fixtures/manual/csv/test_case_addr_01.csv(2)
[IFRIT] PASS ... #1 · 登录
[IFRIT] PASS ... #2 · 地址列表验证
[IFRIT] ── 执行结果 ── status=PASS passed=2 failed=0 exit=0
[IFRIT] 报告=reports/runs/20260602_120000/html/index.html
```

---

## 6. 方式 B：AI 一键生成并运行（推荐）

这是**无技术背景用户**最常用的完整流程：**文档 → 生成 CSV → 带鉴权执行**。

### 6.1 准备接口文档

任选一种来源：

| 来源 | 示例 |
|------|------|
| 本地 Swagger JSON | `api_docs/apispec_1.json` |
| 内网 Swagger URL | `http://192.168.31.129:5000/apispec_1.json` |
| Apifox 分享链接（Markdown） | `https://bebik4pjja.apifox.cn/467309892e0.md` |

### 6.2 生成用例（本地文档）

只生成「地址相关」接口（路径包含 `/api/address`）：

```bash
python main.py --ai-generate ^
  --input-doc api_docs/apispec_1.json ^
  --swagger-endpoint /api/address ^
  --output-format csv ^
  --output-dir fixtures/ai/csv
```

> Windows CMD 用 `^` 换行；PowerShell 可写成一行。

**一行版：**

```bash
python main.py --ai-generate --input-doc api_docs/apispec_1.json --swagger-endpoint /api/address --output-format csv --output-dir fixtures/ai/csv
```

### 6.3 生成用例（远程 URL，无需先下载）

```bash
python main.py --ai-generate --input-url http://192.168.31.129:5000/apispec_1.json --swagger-endpoint /api/address/add --output-format csv
```

Apifox 链接示例：

```bash
python main.py --ai-generate --input-url https://bebik4pjja.apifox.cn/467309892e0.md --swagger-endpoint /api/address/add --output-format csv
```

生成成功后控制台会提示：

```
[IFRIT] AI用例已保存=fixtures/ai/csv/ai_apispec_1_20260602_171345.csv 条数=41 skill=case_generation
```

文件保存在 `fixtures/ai/csv/`，文件名带时间戳。

### 6.4 运行 AI 生成的用例（必须加全局鉴权）

```bash
python main.py --file fixtures/ai/csv/ai_apispec_1_20260602_171345.csv --global-auth --suite ai --generate-report
```

将文件名换成你实际生成的 CSV。

### 6.5 跑「地址业务链路」示例（已内置 4 步）

项目已提供可直接运行的业务流用例：

`fixtures/ai/csv/ai_address_business.csv`

步骤：添加地址 → 查询列表 → 更新 → 删除（自动传递 `address_id`）

```bash
python main.py --file fixtures/ai/csv/ai_address_business.csv --global-auth --generate-report
```

预期：**4 passed**。

### 6.6 AI 生成完整流程图

```
┌─────────────┐     ┌──────────────┐     ┌─────────────────┐
│ 准备 API 文档 │ ──► │ --ai-generate │ ──► │ fixtures/ai/csv │
│ (本地或 URL)  │     │  生成 CSV     │     │   生成结果文件   │
└─────────────┘     └──────────────┘     └────────┬────────┘
                                                   │
                                                   ▼
                                          ┌─────────────────┐
                                          │ --global-auth   │
                                          │  执行 + 报告     │
                                          └─────────────────┘
```

---

## 7. 方式 C：AI 交互模式（像聊天一样操作）

适合：不熟悉长命令、想一步步确认的用户。

### 7.1 进入交互界面

```bash
python main.py --chat
```

出现 `ifrit>` 提示符后，按顺序输入：

```
ifrit> url http://192.168.31.129:5000/apispec_1.json
ifrit> endpoint /api/address/add
ifrit> out fixtures/ai/csv
ifrit> generate
ifrit> exit
```

### 7.2 常用交互命令

| 命令 | 作用 |
|------|------|
| `help` | 显示帮助 |
| `doc api_docs/apispec_1.json` | 指定本地文档 |
| `url https://...` | 指定远程文档 |
| `endpoint /api/address` | 只生成某类接口（可多次输入） |
| `clear-endpoints` | 清空端点过滤 |
| `format csv` | 输出格式 |
| `out fixtures/ai/csv` | 输出目录 |
| `skill case_generation` | 指定 Skill（一般用默认即可） |
| `show` | 查看当前配置 |
| `generate` | 开始 AI 生成 |
| `exit` | 退出 |

### 7.3 单行模式（不进入 REPL）

```bash
python main.py --chat -- doc api_docs/apispec_1.json endpoint /api/address generate
```

---

## 8. 冒烟测试（最快验证环境）

不涉及 AI、不涉及复杂鉴权，**30 秒验证框架和网络**：

```bash
python main.py --file fixtures/smoke/csv/api_test_smoke.csv
```

或跑 manual + smoke：

```bash
python main.py --type csv --suite all
```

---

## 9. 查看测试报告

每次运行会在 `reports/runs/<时间戳>/` 下生成报告。

- **HTML 报告：** `reports/runs/<时间戳>/html/index.html`（用浏览器打开）
- **最新一次：** 看 `reports/latest.txt` 里的 run 编号

控制台最后一行也会提示，例如：

```
[IFRIT] 报告=reports/runs/20260602_172601/html/index.html
```

### 在线浏览报告（需安装 Allure 命令行）

```bash
python main.py --serve-report
```

---

## 10. 清理日志与旧报告

配置文件：`config/settings/app.ini` → `[retention]`

```bash
# 预览将删除哪些（不真删）
python main.py --clean logs --dry-run
python main.py --clean reports --dry-run

# 实际清理
python main.py --clean all
python main.py --clean all --keep-days 7
```

---

## 11. 常见问题 FAQ

### Q1：提示「AI 配置验证失败」

- 检查 `.env` 是否配置了 `OPENAI_API_KEY`
- 检查 `config/settings/ai.ini` 里 `base_url` 是否可达

### Q2：AI 生成很慢或报 429

- 大模型有频率/配额限制，可换模型或第二天再试
- 缩小范围：`--swagger-endpoint /api/address/add` 只生成单个接口

### Q3：跑 AI 用例全部 401

- **请加 `--global-auth`**
- 确认 `auth.ini` 账号密码正确（test/123456）

### Q4：manual 用例第一条登录失败

- 检查 CSV 里 username/password 是否与系统一致
- 检查 `base_url` 是否正确

### Q5：更新/删除地址失败，提示「缺少地址ID」

- 需先跑「添加地址」并成功提取 `address_id`
- 使用 `ai_address_business.csv` 这类**按顺序编排**的用例文件
- 加 `--global-auth` 运行

### Q6：Windows 下 `python` 找不到

改用：

```bash
py -3.10 main.py --type csv
```

### Q7：想看详细请求日志

打开 `logs/daily/daily_当天日期.log`

---

## 12. 命令速查表

| 我想… | 命令 |
|--------|------|
| 验证环境 | `python main.py --file fixtures/smoke/csv/api_test_smoke.csv` |
| 跑人工用例（CSV 内含登录） | `python main.py --type csv --suite manual` |
| 跑人工用例 + 全局登录 | `python main.py --type csv --suite manual --global-auth` |
| AI 生成（本地 Swagger） | `python main.py --ai-generate --input-doc api_docs/apispec_1.json --swagger-endpoint /api/address --output-format csv` |
| AI 生成（远程 URL） | `python main.py --ai-generate --input-url http://192.168.31.129:5000/apispec_1.json --swagger-endpoint /api/address` |
| AI 交互生成 | `python main.py --chat` |
| 跑 AI 用例 | `python main.py --file fixtures/ai/csv/你的文件.csv --global-auth --suite ai` |
| 跑地址业务 4 步 | `python main.py --file fixtures/ai/csv/ai_address_business.csv --global-auth --generate-report` |
| 查看报告 | 浏览器打开 `reports/runs/.../html/index.html` |
| 清理旧报告 | `python main.py --clean reports` |

---

## 附录：给完全新手的「复制即用」脚本

**Windows（PowerShell）— 从生成到跑通地址业务：**

```powershell
cd D:\CodeFiles\EduFiles\ifrit-apitest
pip install -r requirements.txt
copy .env.example .env
# 编辑 .env 填入 OPENAI_API_KEY 后保存

python main.py --ai-generate --input-doc api_docs/apispec_1.json --swagger-endpoint /api/address --output-format csv --output-dir fixtures/ai/csv
python main.py --file fixtures/ai/csv/ai_address_business.csv --global-auth --generate-report
```

第二条命令不依赖 AI，可直接运行验证鉴权与地址接口；第一条需配置好 LLM。

---

**文档版本：** 2026-06-02  
**更多技术细节：** [README.md](README.md) · [__docs/AI_GUIDE.md](__docs/AI_GUIDE.md) · [agent/README.md](agent/README.md)
